/**
 * `server-only` のテスト用スタブ。
 *
 * 本物の `server-only` は、クライアント環境から読み込まれた時点で例外を投げる。
 * Vitest は Node 環境で走るがクライアント側の条件解決が選ばれてしまうため、
 * テストではこの空モジュールへ差し替える。
 *
 * 差し替えの範囲はテスト実行時だけで、ビルド成果物には影響しない
 * （本番のバンドルではサーバー専用の保護がそのまま効く）。
 */
export {};
